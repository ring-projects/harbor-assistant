{
  "name": "harbor-assistant",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.3.5",
  "engines": {
    "bun": ">=1.3.5"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "init:harbor": "bun scripts/init.mjs",
    "dev": "bun run dev:web",
    "dev:web": "bun run init:harbor && bun run --cwd apps/web dev",
    "build:web": "bun run --cwd apps/web build",
    "start:web": "bun run --cwd apps/web start",
    "lint": "bun run lint:web && bun run lint:executor",
    "lint:web": "bun run --cwd apps/web lint",
    "lint:executor": "bun run --cwd apps/executor-service lint",
    "typecheck": "bun run typecheck:web && bun run typecheck:executor",
    "typecheck:web": "bun run --cwd apps/web typecheck",
    "typecheck:executor": "bun run --cwd apps/executor-service typecheck",
    "db:generate": "bun run --cwd apps/web db:generate",
    "db:migrate:dev": "bun run --cwd apps/web db:migrate:dev",
    "db:migrate:deploy": "bun run --cwd apps/web db:migrate:deploy",
    "db:studio": "bun run --cwd apps/web db:studio",
    "dev:executor": "bun run --cwd apps/executor-service dev",
    "start:executor": "bun run --cwd apps/executor-service start",
    "format": "bunx prettier --write .",
    "format:check": "bunx prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "prettier-plugin-tailwindcss": "^0.7.2"
  },
  "ignoreScripts": [
    "sharp",
    "unrs-resolver"
  ],
  "trustedDependencies": [
    "@prisma/client",
    "@prisma/engines",
    "prisma",
    "sharp",
    "unrs-resolver"
  ]
}
